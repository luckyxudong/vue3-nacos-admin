## 1. 创建菜单配置Store

- [x] 1.1 创建 `src/stores/modules/menu.ts` 文件
  - 定义菜单项类型（MenuItem），包含title、icon、path、children等字段
  - 定义Mock菜单数据，包含多级菜单结构（概览、演示、示例、系统管理、项目、关于）
  - 实现菜单展开/折叠状态管理
  - 实现菜单激活状态管理（基于当前路由）
  - 添加菜单查找方法（根据path查找菜单项）

- [x] 1.2 在 `src/stores/index.ts` 中导出菜单Store
  - 确保菜单Store可以被其他模块导入使用

## 2. 创建Tab管理Store

- [x] 2.1 创建 `src/stores/modules/tabs.ts` 文件
  - 定义Tab类型（TabItem），包含title、path、closable等字段
  - 实现Tab列表管理（tabs数组）
  - 实现当前激活Tab管理（activeTab）
  - 实现addTab方法（添加页面到Tab列表）
  - 实现removeTab方法（关闭指定Tab）
  - 实现setActiveTab方法（切换激活的Tab）
  - 实现路由监听，自动添加Tab

- [x] 2.2 在 `src/stores/index.ts` 中导出Tab Store
  - 确保Tab Store可以被其他模块导入使用

## 3. 创建左侧菜单组件

- [x] 3.1 创建 `src/components/layout/Sidebar.vue` 组件
  - 使用shadcn-vue的Card组件作为容器
  - 显示Logo和项目名称
  - 渲染菜单列表（支持多级菜单递归渲染）
  - 菜单项使用Icon组件显示图标
  - 实现菜单展开/折叠功能（点击父菜单展开/折叠子菜单）
  - 实现菜单折叠状态（整个侧边栏折叠）
  - 高亮当前激活的菜单项（基于当前路由）
  - 菜单项点击跳转到对应路由
  - 使用UnoCSS和SCSS变量保持UI风格一致

## 4. 创建顶部工具栏组件

- [x] 4.1 创建 `src/components/layout/Header.vue` 组件
  - **面包屑导航**：
    - 基于当前路由生成面包屑路径
    - 使用菜单配置获取菜单标题
    - 显示格式：首页 > 概览 > 工作台
    - 支持点击面包屑项跳转
  - **主题切换**：
    - 使用现有的`useDark` hook
    - 显示太阳/月亮图标切换
    - 点击切换主题
  - **语言切换**：
    - 使用现有的i18n系统
    - 显示当前语言标识
    - 点击切换语言
  - **用户头像**：
    - 从`useAuthStore`获取用户信息
    - 显示用户头像（默认头像或用户名首字母）
    - 点击显示下拉菜单（登出等操作）
  - 使用UnoCSS工具类和SCSS变量

## 5. 创建页面Tab组件

- [x] 5.1 创建 `src/components/layout/Tabs.vue` 组件
  - 显示已打开的页面列表
  - 每个Tab显示页面标题和关闭按钮
  - 支持点击Tab切换页面（调用router.push）
  - 支持关闭Tab（调用removeTab方法）
  - 关闭Tab后自动切换到相邻Tab
  - 高亮当前激活的Tab（基于当前路由）
  - 使用shadcn-vue的Button组件
  - 首页Tab不可关闭

## 6. 创建"早安"卡片组件

- [x] 6.1 创建 `src/components/layout/GoodMorningCard.vue` 组件
  - 使用Card组件作为容器
  - 显示问候语："早安, {用户名}, 开始您一天的工作吧!"
  - 显示天气信息："今日晴, 20°C - 32°C!"（mock数据）
  - 显示卡通头像或图标
  - 样式符合项目UI风格
  - 添加国际化支持

## 7. 重构主布局

- [x] 7.1 重构 `src/layouts/default.vue`
  - 实现布局结构：
    - 左侧固定宽度菜单栏（Sidebar组件）
    - 右侧主内容区（flex布局）
    - 顶部工具栏固定在顶部（Header组件）
    - Tab栏在工具栏下方（Tabs组件）
    - 主内容区可滚动（router-view）
  - 集成所有布局组件
  - 实现响应式设计（移动端菜单可折叠为抽屉式）
  - 使用UnoCSS工具类进行布局

## 8. 更新首页

- [x] 8.1 更新 `src/pages/index.vue`
  - 在页面顶部添加"早安"卡片组件
  - 保留现有的首页内容
  - 调整布局，确保卡片和内容合理排列
  - 移除原有的语言切换按钮（已集成到Header）

## 9. 添加国际化文本

- [x] 9.1 更新 `src/i18n/locales/zh-CN.ts`
  - 添加菜单项文本（概览、分析页、工作台、演示、示例、系统管理、项目、关于）
  - 添加工具栏文本（面包屑、主题、语言、用户等）
  - 添加"早安"卡片文本（问候语、天气信息）
  - 添加Tab相关文本

- [x] 9.2 更新 `src/i18n/locales/en-US.ts`
  - 添加对应的英文文本
  - 确保所有新增文本都有英文翻译

## 10. 测试验证

- [x] 10.1 功能测试
  - 测试菜单展开/折叠功能
  - 测试菜单点击跳转功能
  - 测试Tab添加、切换、关闭功能
  - 测试面包屑导航功能
  - 测试主题切换功能
  - 测试语言切换功能
  - 测试用户头像下拉菜单功能

- [x] 10.2 UI测试
  - 验证布局在不同屏幕尺寸下的显示效果
  - 验证深色/浅色主题下的显示效果
  - 验证所有组件的样式符合项目UI风格

- [x] 10.3 集成测试
  - 验证与路由系统的集成
  - 验证与状态管理系统的集成
  - 验证与国际化的集成
